# Ruchat TODO

Last updated: 2026-08-03

## High Priority

### 1. Configuration & CLI Improvements
- [~] Environment variable support for Chroma/Ollama settings — added `OLLAMA_SERVER` (parity with the existing `CHROMA_SERVER`/`CHROMA_TOKEN`) and `CHROMA_TENANT`/`CHROMA_DATABASE`. Deliberately did NOT env-var every flag (e.g. `--temperature`/`--top-k`/`--seed`/model selection) — those are per-invocation tuning, not deployment config, and `--options <JSON|file>` already exists as the mechanism for persisting generation-parameter presets; env-var-ifying them would be scope creep with no real usage pattern behind it.
- [ ] Deprecate/phase out scattered JSON string hacks in favor of structured sub-configs — includes the generic per-flag CLI/file merge noted in `cli/serde.rs::load_merged_config`'s comment: today each subcommand's `*Args` struct applies its own CLI flags over the config-file `Value` individually (`update_from_json` per struct); a fully generic merge would need every `*Args` struct to serialize itself to `Value`, which doesn't exist yet. Left as the documented, deliberate deferral it already was — not attempted here to avoid exactly the "implemented speculatively" risk that comment warns against.

### 2. Error Handling & Logging
- [ ] **Not done, much larger scope**: `RuChatError::InternalError(String)`/`Is(String)` are used as catch-all buckets at ~85 call sites across `src/core`/`src/providers`, flattening distinct failure causes into two generic variants and losing the ability to pattern-match on specific failure kinds. Fixing this properly means auditing each call site to decide its own dedicated variant (or a `#[source]`-carrying wrapper) — a large, higher-risk pass, not attempted here.
- [~] Implement graceful degradation when Ollama/Chroma are unavailable — Chroma being unreachable during the Librarian's on-demand retrieval (`Stage::Retrieve`) no longer kills the run (see Done section). Still open: Ollama being unreachable from the very start of a run (Architect/Worker's first call) still surfaces whatever raw error `retry_transient!` exhausts to, not necessarily a clear "Ollama isn't running at <address>" message — not attempted, since Architect/Worker genuinely can't proceed without Ollama regardless of message clarity, this was judged lower-impact than the Chroma case.

### 3. Agent Orchestration
- [ ] Make agent pipeline fully configurable via JSON (the `Stage` sequence in `orchestrator.rs` is still fixed in code, not data — see `ROADMAP.md` Phase 3)
- [ ] Improve Librarian → Worker document injection further (per-document summarization before Worker, multi-collection queries — reranking/relevance scoring is done, see `providers/vector/chroma/rerank.rs`)
- [ ] **Resumable/crash-resilient runs** (checkpointed `Context`) — identified 2026-08-03 while refreshing `comparisons/*.md`: a killed/crashed run currently restarts from `Stage::Scope` with no way to resume. Scope as a local checkpoint file (persist `Context` — turns, round, pending patch — after each stage transition) + a `--resume` flag, not a distributed durable-execution engine. See `ROADMAP.md` Phase 3.
- [ ] **Interactive human-in-the-loop approval gate** — identified 2026-08-03 alongside the above: the only approval mechanism today is automated Critics (an LLM-driven gate) or post-hoc review of the committed branch, no interactive mid-run pause. Add an optional pause (e.g. before `Stage::Commit`) that prints the pending plan/diff and waits for an explicit terminal y/n. See `ROADMAP.md` Phase 3.

### 4. TUI Chat
**Reality check (2026-08-03): there is no interactive chat TUI in the codebase right
now.** The crossterm-based interactive layer (cursor movement, text selection,
history/undo-redo editing — `providers/llm/ollama/chat/{conversation_tree,history,
pos,event_result}.rs`, ~1,260 lines) was deleted 2026-07-31 (`ad0708d "remove more old
code"` and the two commits around it), a few days before the items below were last
touched. `src/tui/` today is just `io.rs` (async stdin/stdout wrapper) and
`render.rs` (a streaming ANSI-colored line renderer for `pipe`/`ask`/`manager run`
output) — 175 lines total, no cursor/selection/editing code at all. The items below
describe bugs in a subsystem that no longer exists; they'd need to be rebuilt from
scratch, not "fixed." Leaving them here as a record of what an interactive TUI would
need if one gets rebuilt, not as active bugs.
- [ ] ~~Fix redraw artifacts and cursor handling edge cases~~ — moot, no cursor handling exists
- [ ] ~~Improve selection + copy/paste reliability~~ — moot, no selection exists
- [ ] ~~Add syntax highlighting for code blocks in chat view~~ — moot, no chat view exists
- [ ] ~~Support multi-line editing with proper indentation~~ — moot, no editable input buffer exists
- [ ] ~~Add command palette / key bindings help screen~~ — moot, nothing to bind keys to
- [ ] `crossterm` (`Cargo.toml`) is now an unused dependency — nothing in `src/` references it. Left in place rather than removed unilaterally, since removing it forecloses rebuilding the interactive TUI without re-adding it; worth a deliberate decision (drop it vs. keep it for a planned rebuild) rather than a silent removal.
- [x] ~~Wire up an actual producer for `AgentEvent::Progress`~~ — done, see Done section below.

## Medium Priority

### Code Quality & Maintainability
- [ ] Add integration tests for full agentic flows (using test Ollama/Chroma) — `agent_debug/*.json` already contain ready-made stage sequences (`architect_only`, `worker_and_validator_rejection`, `multiple_critics`, etc.); wire these into `cargo test` against a mocked `LlmClient`/`VectorStore` instead of writing fixtures from scratch
- [ ] Consistent error handling across Chroma subcommands
- [ ] Refactor duplicated JSON update logic (`update_from_json` methods)
- [ ] Three pre-existing test failures are `#[ignore]`d with reasons rather than fixed (deeper logic bugs, not just stale field names — need someone with context on the intended behavior): `chroma::metadata::tests::test_get_metadata_valid` (`parse_metadata` doesn't support the `key:value,key:value` shorthand the test expects), `chroma::tests::test_create_table` (expects a `"DOCUMENT"` header but rendering only ever emits the short `"DOC"` alias), `chroma::tests::test_json_output` (fixture uses an `Include` value the current enum doesn't accept)
- [ ] The ~16 pre-existing dead-code warnings (`cargo clippy --lib`) aren't blocking CI yet — worth cleaning up so a future `-D warnings` gate is actually adoptable
- [ ] `cargo fmt --check` currently flags formatting drift in ~76 files repo-wide (no custom `rustfmt.toml`, so this is default-rustfmt drift accumulated over time, not a style disagreement) — needs a dedicated `cargo fmt` pass across the repo before a `fmt --check` CI gate can be added without blocking unrelated PRs on pre-existing drift

### Chroma / RAG
- [ ] Support automatic collection creation from `db_config.json` on first embed
- [ ] Add progress bar for large embedding jobs
- [ ] Implement caching layer for repeated file embeddings
- [ ] Add `ruchat chroma-import` command for git history / source trees
- [ ] Better metadata normalization and type safety
- [ ] `embed_script.sh`'s ctags chunk-boundary detection has two open `FIXME: improve per lang/kind handling here` markers around its closing-brace search — the language/kind match lists (Rust, Sh, TOML, Markdown) are hand-maintained and incomplete, so other ctags-supported languages fall back to a single-line chunk instead of the real symbol extent

### Performance
- [ ] Streaming response handling in agent orchestrator (currently buffers)
- [ ] Optimize history limit calculation and token counting
- [ ] Review `reqwest` feature flags in `Cargo.toml`

### Security & Production Readiness
- [~] Never log sensitive data (tokens, prompts with secrets) — audited `src/` for tracing/println calls that interpolate raw config strings or `{:?}`-dump config values; found and fixed the two real instances (see Done section: `orchestrator.rs`'s Librarian `chroma_client` parse-failure log and `ask.rs`'s `--agentic` parse-failure log both used to echo the raw config string, which can legitimately embed `chroma_token`). No other call site found doing this. Left `[~]` rather than `[x]` since this is an ongoing practice for new code, not a one-time fix — no dedicated lint enforces it.
- [ ] Add optional authentication for Ollama
- [ ] Rate limiting / retry backoff configuration

## Low Priority / Nice-to-have

- [ ] API versioning for future breaking changes (`/v1/`)
- [ ] Plugin system for custom tools and agents
- [ ] Web UI / server mode
- [ ] Export conversation as Markdown / JSON
- [ ] Voice input / output support
- [ ] Multi-modal support (images via `qwen2.5vl`, etc.)

## Done / Recently Completed

- [x] Multi-collection queries (2026-08-04, ROADMAP.md Phase 2/v0.3.0 "Improved RAG" item, maintainer: "work on roadmap 0.3 items"). `Query`'s `collection: ChromaCollectionConfigArgs` (single name) became `collection: Vec<String>` — the Librarian's JSON query schema (`agent_role/librarian.md`) now accepts `"collection": string | [string, string, ...]`, so a goal genuinely needing both source code and its commit history (say) can search `repo_src`/`repo_hist` in one query instead of the Librarian being forced to pick just one. Each named collection is queried, filtered, and reranked **independently** for the full `n_results` (maintainer's explicit choice over splitting the budget across collections — a collection with only weak matches shouldn't crowd out a strong one elsewhere), then rendered as its own block labeled `### Collection: <name>` and concatenated. Deliberately not merged into one `QueryResponse` before rendering: different collections can have entirely incompatible metadata schemas (confirmed live — `repo_src`'s `file`/`kind`/`language` vs `repo_hist`'s `commit`/`author`/`date`), so there's no single row shape to merge into. The one embedding call still runs once outside the per-collection loop (same query text every time). `ChromaCollectionConfigArgs::update_from_json` became dead code as a direct result (it was `Query`'s only caller) and was deleted rather than left orphaned.
  - Verified live against a real running Chroma instance: `chroma-query -c repo_src-...,repo_hist-...` correctly queried and labeled both collections with their real (very different) metadata; a single-collection query renders exactly as before (no label), confirmed via `query_with_a_single_collection_has_no_collection_label`. `query_with_multiple_collections_queries_each_one_and_labels_each_block` (via the existing `RecordingVectorStore` test double) is the regression canary, confirmed to catch the regression by temporarily reverting the multi-collection loop to only query the first name and watching the test fail before restoring the fix.
  - **Found and fixed a real, previously-unknown, unrelated bug while smoke-testing this**: `ruchat chroma-query`, `chroma-get`, `retrieve`, and `embed` all panicked on *every* invocation in debug builds (clap's short-flag-uniqueness `debug_assert`), and had undefined `-`-flag behavior in release builds — duplicate short flags where two flattened arg structs each auto-derived or explicitly claimed the same letter (`-s` for both `server`/`sort`; `-i` for both `ids`/`include`, in three separate files; `-o` for both `offset`/`options`; `-M` for both `mode`/`metadata`). `retrieve.rs` additionally had four `requires = "mode=get\|query\|search"` clap attributes that aren't valid clap syntax at all (`requires` takes another arg's name, not a `field=value` condition) — `determine_mode` already does the actual runtime validation, so these were dead weight, not load-bearing, and were removed rather than reimplemented via `requires_if`. All fixed by making the narrower/less-established flag long-only, keeping the broader one's short letter. Verified every one of the 19 CLI subcommands' `--help` now runs cleanly in a debug build (none did, systematically, before — this had apparently never been exercised in a debug build until now). None of this was caught by `cargo test --lib` (clap's argument-graph validation only runs when a command is actually parsed/built, e.g. via `--help`, not during `cargo test`'s unit-test compilation) — worth a `TODO.md` note for whoever eventually adds CI (`.github/workflows/` is still absent per this repo's own doc note): a smoke step that runs `--help` for every subcommand would have caught this class of bug much earlier.

- [x] Closed the real gap in fixed-sequence debug mode's `cargo test` coverage (2026-08-04, ROADMAP.md Phase 2/v0.3.0 "Debug mode improvements" item, maintainer: "work on roadmap 0.3 items"). While investigating this item, found that ROADMAP.md's claim — "isn't wired into `cargo test` yet" — was stale: `debug_stage_machine` (the code path the real `--debug-sequence` CLI flag drives) was already exercised by `cargo test --lib` via the existing `run_fixture`/`build_test_orchestrator` helpers, fully offline via `FakeLlmClient`. The actual gap was narrower: of the 11 `agent_debug/*.json` fixture files on disk, only 9 had a dedicated test driving them through `run_fixture` — `architect_librarian_and_worker.json` and `architect_librarian_worker_validator.json` existed but were never actually run by anything, so a passing `cargo test --lib` gave no signal about whether those two specific role sequences worked. Added `architect_librarian_and_worker_completes`/`architect_librarian_worker_validator_completes`, following the exact same pattern as the other 9 (scripted `FakeLlmClient` responses, one per role in the sequence, `Some(fake_query_response())` for the Librarian's Chroma call) — all 11 fixtures now covered. Still open: breakpoint support (pausing mid-sequence for interactive step-by-step inspection) — no plumbing exists for this yet, a genuinely separate, larger feature from the coverage-gap fix above.

- [x] Automatic collection management: new `ruchat chroma-init` subcommand (2026-08-04, ROADMAP.md Phase 2/v0.3.0 item, maintainer: "work on roadmap 0.3 items"). Reads a `db_config.json`-shaped file (`collections: [{name, description, embedding_model, ...}]` — the repo's own `db_config.json` is exactly this shape, previously pure documentation for the Librarian's prompt via `Context::read_config_file`, never parsed programmatically) and calls `get_or_create_collection` for each entry, instead of a manual `chroma-create` per collection. New `providers/vector/chroma/init.rs`: `DbConfig`/`CollectionSpec` only model the three fields actually needed (`name`/`description`/`embedding_model`) — `example_queries`/`notes_on_metadata`/`metadata_keys` are Librarian-prompt-facing and deliberately not modeled, serde just ignores the extra fields. `description`/`embedding_model` are round-tripped as collection-level Chroma metadata (visible via `chroma-ls`) when present. Mirrors `ChromaCreateArgs::create`'s existing "ensure the database exists first" step for consistency.
  - Verified live against a real running Chroma instance (not just unit tests): ran `chroma-init` against this repo's actual `db_config.json`, confirmed both collections got created/ensured, confirmed re-running is a true no-op (idempotent), and confirmed via `chroma-ls -j` on a genuinely new throwaway collection that `description`/`embedding_model` metadata attaches correctly on creation. One real, worth-documenting nuance found in the process (not a bug): `get_or_create_collection` does **not** update metadata on a collection that already exists — the repo's real `repo_src-all-minilm_l6-v2` collection (created earlier by `ruchat index`, already carrying its own `{"model": "all-minilm:l6-v2"}` metadata) kept that metadata unchanged after `chroma-init` ran, rather than gaining `description`/`embedding_model` on top. This matches Chroma's own get-or-create semantics (an existing collection's config isn't silently overwritten) and is the *safe* behavior for an idempotent init command, but is worth knowing: `chroma-init`'s metadata only actually lands for genuinely new collections.
  - Tests: `DbConfig`/`CollectionSpec` parsing (the exact shipped `db_config.json` shape including unused fields, and a bare name-only entry) and `collection_metadata`'s Some/None construction — 4 new tests, all offline (no Chroma server needed to validate the parsing/metadata-building logic itself).

- [x] Added Anthropic (Claude) as an opt-in chat-model provider (2026-08-04, maintainer request: "I'd like to have integration with claude"). Scoped narrowly and deliberately: **chat only** — Anthropic has no embeddings API, so RAG/`memorize`/recall stay on Ollama unconditionally regardless of this flag. Two crates the maintainer flagged (`octomind`/its provider layer `octolib`, and `agent-client-protocol`) were researched and not used: `octomind` is a full competing ~60-dependency CLI assistant, not a library to embed; `octolib` (~25 deps, including ML libs like `candle`/`tokenizers` never needed here) is lighter but pre-1.0, pins a different `reqwest` version, and showed no evidence of streaming support in its docs — a real regression given every role's response streams token-by-token today; `agent-client-protocol` solves an unrelated problem (editor↔agent, not agent↔LLM-provider). Used instead: a hand-rolled client on `reqwest` (already a dependency) plus the small, mature `eventsource-stream` crate (31KB, ~770 LOC, 455 dependents) for SSE parsing.
  - **`Orchestrator`'s single shared `ollama: Arc<dyn LlmClient>` field split into two**: `chat` (swappable — Ollama or Anthropic) and `embed` (always Ollama-backed, no exceptions). Every existing call site was already unambiguously one or the other (`query_stream`/`chat_stream` → `chat`; `generate_embeddings`/`Query::query` → `embed`), so this was a mechanical, behavior-preserving split once identified — confirmed by the full existing fixture suite passing unchanged with both fields pointing at the same fake client.
  - New `providers/llm/anthropic/` module: `anthropic.rs` (`AnthropicArgs` — CLI/config surface, all fields `anthropic_*`-prefixed/`--anthropic-*` flagged since `AnthropicArgs` flattens into the same `AskArgs` as `OllamaArgs`'s `ModelArgs`, which already owns plain `model`/`temperature`/`top_p` clap arg ids — reusing those names is an id collision, not just a flag-name one, caught by `cargo test`'s clap debug-assert panicking on `pipe --help`), `client.rs` (`AnthropicClient`, the actual `LlmClient` impl: builds the Messages API request — `system` pulled out as Anthropic's top-level field since ruchat's own message construction only ever sends `[system, user]`, no assistant/tool messages exist in the current single-turn-per-round design — POSTs with streaming enabled, turns the SSE response into the same `ChatMessageResponse` chunk shape the Ollama path produces), `sse.rs` (pure, offline-testable event-to-effect parser: `content_block_delta` text deltas, `message_stop`, a real `error` event mid-stream, everything else ignored).
  - `ruchat pipe`/`ask` gained `--chat-provider ollama|anthropic` (default `ollama`) plus `--anthropic-model`/`--anthropic-api-key` (or `ANTHROPIC_API_KEY`, matching `chroma_token`'s env-var-preferred precedent) `--anthropic-max-tokens`/`--anthropic-temperature`/`--anthropic-top-p`. The per-role default-model fallback (`into_config`'s injection) now resolves against whichever provider is actually selected, not always Ollama's — an Anthropic run needs a Claude model id there, not a stray Ollama tag. `ruchat manager`/Team presets don't get a provider-selection field in this pass (both clients stay the shared Ollama one, preserving current behavior) — deferred, not blocking.
  - Tests: `sse.rs` unit tests (all 5 event-handling paths, offline, static JSON fixtures) and `anthropic.rs`'s `model`/`build_client` resolution-precedence tests (config-over-flag, flag-fallback, clear errors when unset) — 11 new tests total, all offline. `AnthropicClient::chat_stream`'s actual HTTP/request-building path has no dedicated unit test, matching the existing precedent that `ollama_rs::Ollama`'s own `LlmClient` impl doesn't have one either (verified via the orchestrator-level fixture suite instead). Manually smoke-tested end-to-end with a deliberately invalid API key against the real `api.anthropic.com`: got back a clean, structured `authentication_error` (HTTP 401) surfaced without a panic — confirms the request shape (headers, endpoint, JSON body) is correct; a genuine success/streaming-content smoke test needs the maintainer's real key.
  - Docs: `ROADMAP.md` Phase 3's provider-abstraction item marked done for the LLM half (vector-store alternatives split into their own, still-open item), `CLAUDE.md`'s "no cloud dependency" note updated to reflect this is now implemented, not just scoped.

- [x] Added "agentic evals" — tests that a real model's response to a role's actual prompt template behaves as intended, separate from the fast/deterministic suite (2026-08-03, maintainer request: "certain agents are supposed to respond in a certain way, given certain input... these should not be run along with the normal tests... but it should be possible to test this behavior"). New `agent/evals.rs` (`#[cfg(test)] mod evals;` in `agent.rs`), a category distinct from everything else in this crate's test suite: it drives a role's real prompt template against a live Ollama server (not `FakeLlmClient`) with a specific scenario as input, and checks the response against a behavioral expectation. Every test is `#[ignore]`d — run explicitly with `cargo test --lib -- --ignored agent_eval`; model overridable via `RUCHAT_EVAL_MODEL` (default `qwen2.5-coder:14b`). Confirmed `cargo test --lib` (no flags) skips all three (209 passed, 9 ignored — was 6) and doesn't invoke Ollama at all.
  - Three starter evals, each a regression canary for a real bug fixed earlier this session: `agent_eval_validator_rejects_a_narrative_walkthrough` (validator.md's non-answer check), `agent_eval_worker_emits_a_tool_call_not_narration` (worker.md's "never narrate" instruction), `agent_eval_architect_does_not_repeat_a_choice_the_real_content_disproved` (architect.md's "treat revealed real file content as ground truth" fix).
  - Ran all three live against `qwen2.5-coder:14b` (a real Ollama server was already running) to confirm the harness actually works end-to-end, not just compiles. Two passed consistently; the Architect one is **genuinely flaky by design**, not a bug — a live run showed the model correctly abandoning a disproven assumption on one call, then, on a re-run, updating its diff to match the real file signature shown but still (wrongly) concluding the function was unused and targeting it for removal anyway. Both are real model behavior: the scenario shows the file's real *signature* but not a fresh clippy result confirming whether it's still flagged dead code, so "is it actually unused" remains a judgment call the model can get wrong even after correcting the surface-level mistake. This is exactly the kind of thing these evals exist to surface — documented directly in the eval's own comment rather than "fixed" by weakening the assertion, since a red run here is genuine information (the architect.md fix is a partial mitigation, not a guarantee), not a false positive.
  - The refinement work itself is worth recording: the first version of the Architect eval's assertion (`!new_plan.contains("unused function \`parse_key_val\`")`) was too strict — a real run showed the model correctly explaining *why* the old assumption was wrong while still mentioning the symbol by name in that explanation, which the naive assertion flagged as a failure. Rewrote it to check for an actual diff *removal* line naming the symbol, not any mention of it — the concrete signal of "still targeting it," distinguishing correction from repetition.

- [x] Added, then reverted, `replace_in_file` as an alternative to `apply_patch` (added 2026-08-03, reverted same day). The idea: sidestep unified-diff syntax mistakes (bad hunk-header counts, missing headers, hallucinated context) by having the model give an exact existing snippet (`old_string`) and its replacement (`new_string`) instead of a diff. Implemented across `agent/tools.rs` (`ToolName::ReplaceInFile`), `agent/protocol.rs` (`Validation::replace_in_file`), `agent.rs`/`orchestrator.rs` dispatch, `agent/types.rs` trace rendering, and `agent_role/worker.md` guidance, with 11 new tests. **Reverted**: real runs showed no improvement over `apply_patch` (maintainer: "it seems the new replace_in_file is not working any better") — the model's error modes weren't specific to diff syntax, so swapping the write-tool shape didn't help. Not treated as a dead end worth re-attempting without a different underlying idea; `apply_patch` remains the sole write tool.

- [x] Sped up `cargo build` and `ruchat index` (2026-08-03, maintainer question: "ruchat index and cargo build are pretty slow"). Two independent changes:
  - **`.cargo/config.toml`**: switched the linker to `lld` for `x86_64-unknown-linux-gnu` (the only target confirmed to have it installed on this machine) via `rustflags = ["-C", "link-arg=-fuse-ld=lld"]`. Link time is a real chunk of every build for a dependency graph this size (tokio, reqwest, chroma/chroma-types, ollama-rs, ...); this only changes the linker, not codegen, so it's a pure speedup with no behavior change. Verified: builds cleanly, an incremental `touch src/main.rs` + `cargo build --release` relink completed in ~32s.
  - **`ruchat index` (`core/index.rs`) processed every file sequentially** — one `ctags` subprocess spawn, then one Ollama-embed-plus-Chroma-upsert round trip, one file at a time. Split into two bounded-concurrency phases: `CTAGS_CONCURRENCY` (8) for the cheap, independent, CPU-bound ctags+file-read phase via `buffer_unordered`, and a lower `EMBED_CONCURRENCY` (4) for the Ollama/Chroma-bound embed phase via `try_for_each_concurrent` (keeps the existing stop-on-first-error behavior instead of silently continuing past a failure now that it's concurrent). Deliberately not the same concurrency for both: Ollama typically serializes inference per loaded model, so high concurrency there would just queue requests with no throughput benefit — the win is overlapping HTTP/network round-trip latency, not multiplying raw embedding throughput. Existing `tracked_files_under_*` tests still pass unchanged (behavior-preserving refactor); smoke-tested against a throwaway 5-file directory to confirm the parallelized ctags/read phase still produces correct per-file results.

- [x] Fixed memory recall silently querying the wrong Chroma collection, and a repeated-plan stall root cause (2026-08-03, real-run report: `fix_one_clippy_lint` escalated after the Architect repeated an identical plan post-rejection, and the maintainer separately flagged that the "Memory" retrieval content looked unrelated to the task, worried RAG "isn't doing that, right now"). Two distinct bugs found from the same trace:
  - **`recall_prior_memories` always queried the literal collection named "default"**, never whatever `--collection` the run was actually configured with. Unlike `run_librarian_retrieval` (where the Librarian's own LLM picks a collection name as part of its JSON query, guided by `task_hint`), this ad-hoc pre-run recall builds a bare `Query::default()` with no LLM step to pick a collection — and `ChromaCollectionConfigArgs::default()` falls back to the literal string `"default"` when nothing overrides it. `ask.rs`'s `--collection` flag only ever set a natural-language `task_hint` ("Query the X collection"), never anything `recall_prior_memories` could actually read. This explains the irrelevant retrieved content: it wasn't a ranking/relevance problem, memory recall was searching an entirely different, disconnected collection the whole time. Fixed by also setting a plain structured `memory_collection` field on the Librarian's config in `ask.rs`, which `recall_prior_memories` now reads and passes through as the query's `collection`. Regression-pinned with `recall_prior_memories_queries_the_configured_memory_collection`, using a new `RecordingVectorStore` test double (`agent/llm_client.rs`) that captures which collection name a caller actually queried — confirmed it catches the regression (recorded `["default"]`) by reverting the fix and watching it fail.
  - **The Architect repeated an identical plan after a rejection revealed the file's real content didn't match its assumption** — the Worker's `apply_patch` targeted a function it assumed was dead code (per what it believed was a clippy warning), the patch failed because the real file had a completely different, actively-used function there, and the Validator's rejection included the real content (per the existing "show real file content on context mismatch" fix) — but the Architect's next plan repeated the exact same choice anyway, triggering the stall-detection escalation. `agent_role/architect.md` now explicitly instructs the Architect to treat real file content shown in a rejection as ground truth overriding its own prior assumption, and to pick a different, verified target if the original choice no longer holds — not separately unit tested (prompt wording, verified by re-reading the instruction, same as other prompt-only changes in this codebase).

- [x] Run summary: list every contributing issue, wrap at 120 characters (2026-08-03, maintainer feedback: "only the main reason why the agents did not reach a conclusion is printed... several things may have gone wrong... the summary should be line wrapped"). Two changes to `orchestrator/run_summary.rs`:
  - `generate_failure_summary`'s prompt used to ask for "the single main reason" the run failed — rewritten to ask for *every* distinct contributing issue, one per line, most decisive first, discarding the "single reason" framing that was throwing away real information (e.g. an earlier stall that got worked around, followed by the genuinely fatal error) about anything but the last thing that happened.
  - Added a wrapping backstop, same reasoning as `git::wrap_commit_message_body`: models don't reliably honor an exact-line-length instruction on their own. Extracted the existing `wrap_commit_message_body`/`wrap_line` word-boundary wrapper out of `git.rs` into a new shared `utils::text::wrap_line(line, max_len)` (parameterized by width instead of `git.rs`'s hardcoded 80), so both call sites — commit messages at 80 chars, run summaries at 120 (`run_summary::SUMMARY_LINE_LEN`) — share one implementation. Each line of a (possibly multi-line, one-issue-per-line) summary is wrapped independently, not the whole thing as one paragraph, so the line-per-issue structure survives wrapping.
  - Regression-pinned with `generate_failure_summary_wraps_each_line_independently_at_120_chars` (confirmed it catches the regression by reverting the wrapping call and watching the test fail) plus `utils::text::tests` covering the extracted `wrap_line` directly (short lines untouched, word-boundary wrapping, a different `max_len`, a single word longer than `max_len` left intact). The "ask for every issue, not just one" prompt change itself isn't separately testable without a live model — it's prompt wording, verified by re-reading the instruction rather than a unit test, consistent with how prompt-wording changes elsewhere in this codebase are verified.

- [x] Trace readability, part 2: agents whose output triggered no rejection left no trace at all (2026-08-03, maintainer follow-up clarifying the entry below: "I used tool, where I meant agent... the trace only contains the agent output, not the agent actions... unclear from the trace log which agent exactly did what"). The prior fix only covered the Worker's read-only tool-call detour; the same root pattern existed for three other agents, all fixed the same way (push the raw output as a Turn unconditionally, instead of only when something noteworthy — a rejection — happens):
  - **Validator's `VALIDATED` verdict pushed no turn at all** (`Stage::Validate`, `orchestrator.rs`) — only `REJECTED`/unparseable did. A successful validation was streamed live to the console and then vanished from the trace with no record it had even run. Now pushes a `TurnKind::System` turn with the raw verdict.
  - **An approving critic's review pushed no turn at all** (`run_critics_parallel`) — only a rejecting one did; the approving case only reached an ephemeral `ctx.trace(...)` event (visible live on the console/stream at the time, but never added to `ctx.turns`, so gone the next time the trace file gets rewritten). Now pushes a `TurnKind::System` turn labeled `Critic '<name>'`, mirroring the existing rejection turn's shape.
  - **The Scoper's own raw output was only ever recorded in fragments** (`run_scope_stage`) — the `notes` field if non-empty, a rejected-lookup reason, a failed-lookup message — never the actual verdict it produced. A round where the goal was already `READY` with empty notes left zero trace of the Scoper having run. Now records the raw output unconditionally, right after `query_stream`, before any of the existing selective pushes (which still also fire, capturing the same information a second time in a more distilled form — acceptable redundancy for a human-facing trace).
  - Regression-pinned with `run_critics_parallel_records_an_approving_review_as_a_system_turn` and `run_scope_stage_records_its_raw_output_even_with_empty_notes`, both direct-calling the standalone functions (confirmed both catch their regression by reverting the fix and watching the test fail). The Validator fix is inline in `run_stage_machine`'s big match arm (not its own function, same reason `Stage::Implement`'s equivalent fix wasn't independently unit tested either) — verified by code review and the existing fixture suite passing unchanged.

- [x] Trace readability: missing tool-call actions, unreadable `apply_patch` diffs (2026-08-03, maintainer feedback: "on the console only the actions are printed... but the trace only contains the tool output, not the tool actions... unclear from the trace log which tool exactly did what"). Two separate gaps, both in how a round's content reaches `ruchat_traces/`, not in what actually happened during the run:
  - **Read-only tool-call actions were never recorded, only their output.** `Stage::Implement` (`orchestrator.rs`) only ever called `ctx.push_turn(TurnKind::Implementation, ...)` once per round, with whichever Worker response was *last* — so when the Worker's first response was a read-only tool call (`read_file`, `git_log`, `cargo_clippy`, ...), that action's own raw text was silently discarded the moment the reask overwrote `ctx.output`; only `handle_structured_tool`'s resulting `TurnKind::Retrieval` turn (the tool's *output*) survived. The trace showed a result with no visible cause. Fixed by pushing an Implementation turn for that first response *before* dispatching it, so the action and its output both appear, in order.
  - **`apply_patch` diffs were unreadable.** A diff lives inside a JSON string field in the Worker's raw output, so its newlines are the two literal characters `\`+`n`, not real line breaks — rendered as-is, an entire multi-hunk diff was one incomprehensible line. Added `render_turn_content_for_trace` (`agent/types.rs`): for an Implementation turn whose content parses as an `apply_patch` tool call, extract the `diff` field (already real-newline text once `tools::parse_tool_call` JSON-parses it — no manual unescaping needed) and re-emit it as its own fenced ` ```diff ` block; everything else passes through unchanged. Wired into `full_history_view()` and a new `context_view_for_trace()` (used only by `trace_body()`'s "## Context" section, which duplicates the latest Plan/Implementation) — deliberately *not* into `context_view()` itself, since that also feeds the PLAN/IMPLEMENTATION prompt variables the model sees, where altering its own prior output could confuse it or break round-tripping.
  - Regression-pinned with `trace_body_renders_an_apply_patch_diff_with_real_newlines`/`trace_body_leaves_non_apply_patch_content_unchanged` (confirmed the first catches the regression by reverting the fix and watching it fail). The missing-action-turn fix itself is a one-line addition inline in `run_stage_machine`'s large match arm — like the proactive System-turn reminder added earlier in this same arm, not independently unit tested (would need a live Worker round through the real, non-fixture-driven code path), verified instead by code review and by the existing `librarian_and_worker_completes`/`worker_and_validator_completes` fixture tests still passing unchanged.

- [x] Fixed `CONTAINS` on scalar metadata fields always returning zero rows (2026-08-03, found while verifying the repo_src reindex — `file CONTAINS 'cli'`, one of `db_config.json`'s own documented example queries, matched nothing). Root cause: `CONTAINS`/`NOT CONTAINS` always compiles to Chroma's `MetadataComparison::ArrayContains` (`where.rs::map_sql_comparison`), which is real array-membership only — Chroma's metadata filter language has no scalar-substring operator at all, so a plain string field like `file`/`name` can never match through it server-side, silently (no error, just an always-empty result). Fixed with a client-side evaluator instead of trying to coerce Chroma into doing something it structurally can't: `where.rs` gained `where_needs_client_side_eval` (detects `CONTAINS` anywhere in the expression tree) and `metadata_matches` (a full recursive evaluator for `Where` against one row's metadata — AND/OR composites, primitive comparisons, IN/NOT IN, and a corrected `ArrayContains` that does real substring matching against a scalar `Str` and real membership against genuine array-typed fields, so already-working cases like `references CONTAINS 'x'` keep working, now via the same path). `query.rs::Query::query` (the path the Librarian's live RAG retrieval actually uses) now detects this case, skips sending the doomed filter to Chroma, over-fetches a larger candidate pool from the unfiltered similarity search (`CLIENT_FILTER_OVERFETCH_FACTOR`/`CLIENT_FILTER_MAX_FETCH` — a heuristic, not a guarantee: a filter only a small fraction of the collection satisfies can still under-return versus a real server-side filter), forces `Include::Metadata` into the request so there's something to evaluate against, then filters/truncates the response client-side. Regression-pinned at both layers — `metadata_matches_does_real_substring_matching_on_a_scalar_string_field` (plus AND-combination, NOT CONTAINS negation, and the genuine-array-field case) in `where.rs`, and `query_filters_contains_on_a_scalar_field_client_side` end-to-end in `query.rs` via `FakeVectorStore` — both confirmed to catch the exact regression by reverting the fix and watching the tests fail. **Not yet applied** at the time: `get.rs`/`retrieve.rs`'s get+query modes and `delete.rs` share the identical `WhereArgs` → `Where` → Chroma flow and the identical bug — see the next entry for `get.rs`/`retrieve.rs`; `delete.rs` remains open, tracked above.

- [x] Applied the same fix to `get.rs`/`retrieve.rs` (2026-08-03). Both share `query.rs`'s exact `WhereArgs` → `Where` → Chroma shape and had the identical bug. Rather than duplicate `query.rs`'s `select_indices`/`with_metadata_included` locally a third time, moved them into `where.rs` (alongside the existing `metadata_matches`) as shared `pub(crate)` helpers, and added a new `where.rs::filter_get_response` (the `GetResponse` analog of `query.rs::filter_query_response` — `GetResponse` is a flat `Vec<T>` per field, not `QueryResponse`'s nested-per-query-text `Vec<Vec<T>>`, so it needed its own, simpler filter). `get.rs::GetArgs::get` and `retrieve.rs::RetrieveArgs::execute_get` both now detect `where_needs_client_side_eval`, skip the doomed server-side filter, over-fetch nothing extra (a `get` isn't similarity-ranked, so unlike query mode there's no need for `CLIENT_FILTER_OVERFETCH_FACTOR` — just fetch everything matching `ids`, forcing metadata into `include`, then filter+offset+limit client-side). `retrieve.rs::execute_query` mirrors `query.rs::Query::query` exactly, reusing the now-`pub(crate)` `filter_query_response`/`CLIENT_FILTER_OVERFETCH_FACTOR`/`CLIENT_FILTER_MAX_FETCH` instead of a second copy. No new tests added directly in `get.rs`/`retrieve.rs`: neither file had any pre-existing test harness (unlike `query.rs`, which tests via the `VectorStore` trait's `FakeVectorStore` — `get.rs`/`retrieve.rs` call `ChromaCollection`/`create_client` directly with no fake seam), and the actual filtering logic they now call is already regression-pinned via `where.rs`'s `filter_get_response`/`metadata_matches` tests and `query.rs`'s `filter_query_response`/`query_filters_contains_on_a_scalar_field_client_side` test — introducing a new mockable-client abstraction just to re-test the same already-tested logic was judged out of scope for this pass. Verified with `cargo check --lib --tests` (clean) and `cargo test --lib` (all passing, 9 pre-existing ignores unchanged; the exact pass count has since shifted with unrelated work in the same session — see the `replace_in_file` revert entry below). `delete.rs` needed a structurally different fix (resolve-then-delete-by-ID, not resolve-then-filter) — see the next entry.

- [x] Applied the CONTAINS-safe fix to `delete.rs` (2026-08-03), the last of the three. Unlike get/query, `delete` has no "filter the response afterward" step to fall back on — a `--where "file CONTAINS 'x'"` delete previously deleted nothing at all, silently (safe-by-accident, but silent). Fixed by resolving matching IDs client-side *first* when `where_needs_client_side_eval` is true: a `get` scoped to `ids` if given (else the whole collection), with metadata forced into `include` via `with_metadata_included`, then `where.rs::filter_get_response` (the same helper `get.rs` uses) filters it down and applies `--limit`. The resulting `GetResponse.ids` becomes the explicit ID list handed to `delete(Some(ids), None, None)` — no `where`/`limit` passed to Chroma at that point, since the ID list already is the fully-resolved match set. An empty match set short-circuits with an info log instead of calling `delete` with an empty ID list. The non-CONTAINS path (plain comparisons Chroma can evaluate natively) is untouched — same `collection_handle.delete(ids, where_clause, limit)` call as before. No new tests: `delete.rs` has no pre-existing test harness either (same `ChromaCollection`/`create_client`-direct shape as `get.rs`/`retrieve.rs`, no fake seam), and the filtering logic itself is already regression-pinned in `where.rs`. Verified with `cargo check --lib --tests` (clean) and `cargo test --lib` (201 passed, 0 failed, 9 pre-existing ignores unchanged).

- [x] Extended automatic memory recall to work without a Librarian configured (2026-08-03). Previously `recall_prior_memories` (`orchestrator.rs`) required both `self.client` and `self.librarian` — reusing the Librarian's Chroma client/`embed_model`/`memory_collection` — so a memorize-only run (Worker's `Memorize` tool call, which already writes independently via `Agent::embed`'s own `EmbedArgs`) could write memories it could never recall. Fixed in two parts:
  - `core/embed.rs::EmbedArgs` gained three read-only accessors (`collection_name`, `embed_model_name`, `client`) exposing exactly what recall needs from the same config a `Memorize` write already uses, so recall and write always agree on where memories live. `embed_model_name` deliberately doesn't go through `OllamaArgs::init`'s network-validated model resolution (`list_local_models`) — just a synchronous config read (new `ModelArgs::first_model_name` / `OllamaArgs::model_name_or`) — recall only needs the configured name, not to confirm the model is pulled.
  - `Orchestrator::new` now resolves `self.client` independently from the Worker's `embed_args` whenever no Librarian client was built — but only when the Worker has `embed_args` *explicitly* configured, not `EmbedArgs::default()`'s silent fallback. This gate matters: unlike a `Memorize` call (an explicit action the Worker only takes when it decides to), `recall_prior_memories` runs unconditionally at the start of every single run, so defaulting here would mean every run — including ones with zero interest in Chroma — pays for an attempted connection to `EmbedArgs::default()`'s literal `localhost:8000`/`"default"` collection. (`ChromaClientConfigArgs::create_client` itself never touches the network — it only builds request config — so this gate is about avoiding an unwanted *later* query at recall time, not slow construction.) `recall_prior_memories` itself stays a synchronous, no-network-if-`self.client`-is-`None` read, matching how `build_test_orchestrator`'s hand-built `Orchestrator` literals already inject a fake client directly and keep the fixture suite offline.
  - Regression-pinned with `recall_prior_memories_works_without_a_librarian_via_the_workers_embed_args`, confirmed to catch the regression by reverting the fix (required `self.librarian` again) and watching it fail. The pre-existing `recall_prior_memories_is_a_noop_without_a_librarian` test still passes and now has a comment clarifying it covers "no Chroma client at all," not "no Librarian" specifically — the two are no longer the same case. `Orchestrator::new`'s new fallback-construction branch itself has no dedicated test, matching the existing precedent that its neighboring Librarian-client-construction branch (right above it) isn't independently unit tested either — both are only reachable, in practice, through the higher-level recall test and the existing fixture suite.

- [x] Wired up a real producer for `AgentEvent::Progress` (2026-08-03, TUI Chat section item). `tui/render.rs::render_pipeline_stream` already had a full `Progress(pct)` match arm drawing a "...N%" status line, but nothing ever sent one. `Orchestrator::run_stage_machine`'s `Stage::Plan` arm now sends one per round transition, computed by a new pure `progress_pct(round, max_iterations)` — clamped to 100 (a round can push `ctx.round` past `max_iterations` right before the budget-exhausted check), and returning 100 outright when `max_iterations` is 0 rather than dividing by it. A coarse, monotonically-increasing gauge of proximity to the iteration budget, not a precise ETA — a single slow round (e.g. one with a real `cargo test` run) can still take arbitrarily long. Tested directly as a pure function (zero-before-first-round, linear scaling, clamping past the budget, zero-`max_iterations` case) for the same reason `should_continue_patch_loop` is: exercising this through a live round would need a real Ollama/Chroma round trip. One test needed a follow-up fix: it originally compared `progress_pct`'s f32 result against an independently-computed `100.0/3.0` with exact equality, which fails on the last bit or two since f32 division order isn't guaranteed to round identically — switched to an epsilon comparison.

- [x] Graceful degradation when Chroma is unreachable during Librarian retrieval (2026-08-03, High Priority item 2). Scoped narrowly rather than attempting every possible Ollama/Chroma failure mode: `run_librarian_retrieval`'s on-demand `Stage::Retrieve` lookup used to propagate a Chroma connection failure straight through `?`, killing the entire run via `Stage::Retrieve`'s own `?` in `run_stage_machine` — even though Architect/Worker/Test/Commit don't need RAG context to function, and `recall_prior_memories` already took the graceful-skip stance for the deterministic pre-run recall case. Now a failed `retrieve_and_generate` call logs a `tracing::warn!`, traces a note, and pushes a `TurnKind::System` turn explaining RAG was skipped this round, then the run continues without retrieved context. Deliberately left alone: Ollama itself being unreachable from a run's very first call (Architect/Worker can't proceed without it regardless of message clarity) and a general retry/circuit-breaker framework — both judged lower-impact or out of proportion for this pass; see the remaining note on the High Priority item above. Regression-pinned with `run_librarian_retrieval_degrades_gracefully_when_chroma_is_unreachable`, using a new `FailingVectorStore` fake (`agent/llm_client.rs::fake_vector_store`) simulating a Chroma outage — confirmed the test fails against the pre-fix `?`-propagation and passes after.

- [x] Fixed a regression in the default-model fix immediately above (2026-08-03, real-run report: `bash scripts/refactoring_examples.sh run rename_helper_and_call_sites` failed instantly with "No model specified"). Root cause: `OllamaArgs::init`'s `.max(1)` — meant to make `ruchat index`/`ruchat embed` fall back to a real default when no `-m` was given — forced at least one model-resolution attempt *unconditionally*, including for callers that pass an empty `default` on purpose. `AskArgs::ask` (the `pipe` command) is exactly that: it calls `init("", cfg)` and never uses the returned `models` at all — Architect/Worker models come from `--team-model`/`--validator-model` instead — and relied on an empty `models` Vec being a silent, harmless outcome. With the unconditional `.max(1)`, `get_model(0, "")` ran anyway, resolved nothing, and errored before `pipe` ever reached its own per-role models, breaking every `pipe` invocation (which correctly never passes a top-level `-m`). Fixed by extracting the decision as its own pure function, `resolve_model_slot_count(nr_of_models, default)`, which only forces one slot when `default` is non-empty — restoring `pipe`'s old zero-iteration behavior while keeping `index`/`embed`'s fallback intact. Regression-pinned with three direct tests covering all three cases (empty default stays zero, real default becomes one, already-explicit counts are untouched) — confirmed they catch the exact regression by reintroducing the unconditional `.max(1)` and watching the first one fail. Deliberately network-free: an earlier version of this test called `OllamaArgs::init` directly, which needs a live Ollama server to resolve a model name, and also surfaced a second, pre-existing, separate issue — `ServerArgs::default()`'s `server` field has no clap `default_value` applied (the same class of gap already documented for `OutputArgs`), so a bare `#[derive(Default)]` `ServerArgs` fails to parse as a URL — not fixed here, out of scope for this regression.

- [x] `apply_patch` multi-file diff crash + RAG/embedding pipeline cleanup (2026-08-03, two real-run bug reports). Separate fixes from the same session:
  - **`apply_patch` "orphaned hunk header" crash**: a real run had the Worker concatenate two files' diffs (`src/tui/io.rs` and `src/cli/prompt.rs`) into a single `apply_patch` call instead of calling `apply_patch` twice — `diffy::Patch::from_str` only understands one file per call, so it parsed the first file's hunks then choked on the second file's `--- a/` header as unparseable trailing content, surfacing only a cryptic "orphaned hunk header after trailing content" with nothing actionable. `Validation::apply_patch` (`agent/protocol.rs`) now counts `--- ` headers up front and refuses immediately with a clear message telling the Worker to submit one file per call and call `apply_patch` again afterward for each additional file — the multi-file patch loop already supports exactly that. `agent_role/worker.md` now explicitly documents this (also clarifies the file path comes from the diff's own header, not a separate field — the old wording implied one existed). Regression-pinned with the exact two-file diff from the report.
  - **Librarian retrieval noise/token bloat** (user question: "hashes cost a lot of tokens... unuseful... how can we actually retrieve information that's going to help the response?"). Root cause was two independent issues, both fixed: (1) `chroma.rs`'s `flatten_get`/`flatten_search`/`flatten_query` rendered metadata via `format!("{:?}", map)` — raw Rust Debug enum syntax (`Str("ask")`, `Int(2)`) — instead of each value's own clean JSON form, wasting tokens on wrapper noise for no benefit to the model reading it; replaced with `format_metadata`, which also caps any single oversized value (a generic backstop, not tied to one field) at `MAX_METADATA_VALUE_CHARS`. (2) `embed_script.sh`'s ctags-derived `references` metadata field was an *uncapped* whole-repo `ripgrep` match list (60+ `file:line` entries in the reported case) — capped at a new `max_references=50`. Also found, while investigating: a second, unused native ingestion path (`ruchat index` / `src/core/index.rs`) already existed and handled `references` more sensibly (capped, off by default) — but its own `walk_files` was a raw recursive directory walk with only a small hardcoded skip-list, the *exact* class of bug that caused this repo's real 494MB `tags` file incident (see the ctags entry above) if ever pointed at this repo's root (would sweep in `docs/`). Fixed with `tracked_files_under`, which prefers `git ls-files` (scoped, filtered by extension) whenever the target is a git work tree, falling back to the old walk only outside one. Embeddings themselves were confirmed already stored as native `f32` vectors end-to-end — not a source of overhead, nothing to change there. Actually switching the live `repo_src-all-minilm_l6-v2` collection over to `ruchat index` (deleting/re-ingesting) is a separate, explicitly-confirmed-before-running step, not done as part of this code change.
  - **`ruchat index`/`ruchat embed` CLI bugs found along the way** (user report): `EmbedArgs` flattens both `OllamaArgs`/`ModelArgs` (`--model`) and `UpdateMetadataArrayArgs` (`--metadata`), both auto-deriving the same `-m` short flag — a real collision. `UpdateMetadataArrayArgs` now uses explicit `-M`. Separately, omitting `-m`/`--model` entirely (the common case) failed with "no model specified" even when the caller passed a real default (e.g. "all-minilm:l6-v2") — `ModelArgs::get_model`/`resolve_model_arg` only fell back to the default for an explicit `Some("")`, never for `None` (no `-m` given at all, `self.model` empty), and `OllamaArgs::init`'s loop over `0..get_nr_of_models()` never even ran once when that count was 0. Fixed: `resolve_model_arg` (extracted as a pure, directly-tested function) now treats `None` and `Some("")` identically, and `init` uses `.max(1)` so the loop always runs at least once. Callers with no sensible default (`delete_model`/`pull`, `default = ""`) are unaffected. Verified live: `ruchat index <dir>` with no `-m` now gets all the way to a real Chroma HTTP call instead of failing at model resolution.
  - All four fixes regression-pinned (each confirmed to actually catch its own regression by reverting and watching the test fail): `apply_patch_gives_an_actionable_message_for_a_diff_spanning_two_files`, `format_metadata_uses_clean_json_not_rust_debug_syntax`/`format_metadata_truncates_an_oversized_value`, `tracked_files_under_this_repo_never_includes_docs`/`tracked_files_under_returns_none_outside_a_git_work_tree`, `resolve_model_arg_*` (3 cases). `cargo test --lib`: 171 passed; `cargo clippy --lib --tests`: 71 warnings (unchanged baseline); `cargo check --lib --release`: clean.

- [x] Trace file overhaul (2026-08-03, two rounds of user feedback on the old single `.ruchat_trace.md`). Final design:
  - **Root cause of the original "missing rounds" report**: `Context::trace()` built the file from `history_view(u64::MAX)`, which deliberately excludes `TurnKind::Retrieval` turns (tool output, RAG docs, git log/diff, `cargo_check`/`cargo_clippy`, memory recall) since prompts render those as a separate, round-scoped `DOCUMENTS` section — but `trace()` never rendered that section either, unlike `print_debug_info` (debug-sequence runs only), which already did. Net effect: any round whose only content was a tool call or RAG lookup was invisible in the live trace file, not even collapsed — just gone. Fixed with `full_history_view()` (unfiltered, chronological, all turn kinds) and `trace_body()` (`agent/types.rs`); `history_view` itself is untouched, since the prompt-facing HISTORY/DOCUMENTS split is meaningful to the model, not just a formatting choice. Regression-pinned with `trace_body_includes_retrieval_turns`.
  - **One file per run, never overwritten** (user follow-up: "don't just overwrite the trace summaries... remove the dot before the filename, add an increment in the name, and put the trace files in a dedicated subdirectory, containing successes and failures subdirectories"). The old `.ruchat_trace.md` was a single path every run clobbered, so only the most recent run's trace ever survived. Now: `Context::init_trace_index()` (called once, at the very start of `run_stage_machine`/`debug_stage_machine`) scans `ruchat_traces/` + its `successes/`/`failures/` subdirectories for existing `ruchat_trace_<N>.md` files and picks one past the highest `N` found, so every run gets its own slot. `trace()` refreshes that run's live file at `ruchat_traces/ruchat_trace_<N>.md` on every turn (dot removed, no more hidden file). `.gitignore` updated from `.ruchat_trace.md` to `ruchat_traces/`.
  - **Outcome summary on every run, not just failures** (user follow-up: "also for successful runs there should be a trace summary of how or why it succeeded (not the full trace)"). `run_stage_machine` tracks a `success` flag, set only when `Stage::Commit` actually succeeds. At the end of every run, `Orchestrator::finalize_trace` makes one direct LLM call (`orchestrator/run_summary.rs` — renamed from the original `postmortem.rs` once it started covering the success case too; same one-shot pattern as `git::generate_commit_message`, deliberately bypassing the Agent/Role/Context turn-log machinery) and archives the result, removing the live file: `generate_success_summary` + `Context::finalize_success_trace` write *just the summary* (deliberately not the full trace — a successful run doesn't need the blow-by-blow, and it keeps `successes/` easy to skim) to `ruchat_traces/successes/ruchat_trace_<N>.md`; `generate_failure_summary` + `Context::finalize_failure_trace` write the summary *plus* the full trace body underneath (a failure is exactly when a maintainer needs to dig through every round) to `ruchat_traces/failures/ruchat_trace_<N>.md`. Best-effort throughout: if the LLM call itself fails (Ollama unreachable, timeout, empty response), the run is still archived to the correct directory with a placeholder note instead of orphaning the live file — a diagnostic nicety must never mask or replace the original outcome. New optional `failure_analysis_model` config key, falling back to the Worker's model (always configured), exactly like `commit_message_model` does.
  - Tests: `generate_failure_summary`/`generate_success_summary`'s pure LLM-call logic covered directly via `FakeLlmClient` (trimmed response, empty-trace rejection, empty-response rejection); `parse_trace_index` (the filename parser `init_trace_index`'s directory scan relies on) covered directly for both the well-formed and malformed cases; `finalize_trace_sends_a_failure_trace_event_with_the_analysis`/`..._success_...` drive `Orchestrator::finalize_trace` end-to-end for both outcomes and assert on the `Trace` event sent over the channel rather than reading the archived file back off disk, since `ruchat_traces/` is written concurrently by every other test that calls `query_stream` in the same test binary — reading it back would be a real, if pre-existing, race. Every one of these was confirmed to actually catch its regression (reverting the fix and watching the test fail) before being left in place. The full `run_stage_machine` → `Stage::Escalate`/`Stage::Commit` → `finalize_trace` integration path itself isn't end-to-end tested: reaching a real stall/escalation or a real commit through the full stage machine would mean either multiple real Worker rounds, a real `cargo test`/`cargo check` invocation via `Validation::run_build_and_test`, or real git operations (no injectable path for any of these) — same category of out-of-scope real I/O already noted for the multi-file patch loop and commit-message generation above.

- [x] Prompt-engineering pass over every `agent_role/*.md` template and `Role::build_chat_messages`'s system-prompt construction (2026-08-03, user request: "you are a prompt engineering specialist... apply improvements"). No behavioral bug reported this time — a deliberate quality pass, not a fix. Changes:
  - **`role.rs`**: the "no human available, never ask a question or wait for input" framing previously lived only inside `architect.md`'s and `worker.md`'s own templates (worded slightly differently in each, copy-pasted, and entirely absent from `scoper`/`validator`/`librarian`/`critic`/`summarizer` even though the same constraint applies to all of them). Moved it into the system message built once in `build_chat_messages`, so every role gets it consistently — system-message content is the highest-priority, stickiest instruction slot a chat-tuned model has, and a non-negotiable rule like this belongs there rather than duplicated (or silently missing) across per-role user-turn templates.
  - `agent_role/architect.md` / `agent_role/worker.md`: trimmed the now-redundant autonomy paragraph each had, since it's covered by the system message above.
  - `agent_role/validator.md`: was previously only 8 lines checking a single narrow case (the "asks a question" non-answer check) and never explicitly instructed to check technical correctness at all — it relied solely on `get_task()`'s one-line system framing. Restructured into an explicit two-step check (non-answer first, then technical correctness), and broadened the non-answer definition to also cover narrative/walkthrough prose (numbered steps, "### Identified..." headers), matching the same failure mode fixed at the orchestrator level in the "narrative instead of a tool call" entry above.
  - `agent_role/summarizer.md`: previously just 2 lines (`GOAL`/`RAW HISTORY TO COMPRESS`) with zero guidance on what to keep — the weakest template found in this pass, given its output (`Context::collapse_to_summary`) completely replaces the turn history it's compressing, so information lost here is unrecoverable. Rewritten to explicitly require preserving file paths/symbols/line numbers, every rejected approach and its specific reason, current implementation state, and concrete lookup facts, while discarding filler.
  - `agent_role/critic.md`: added a specificity requirement (reject with the exact file/line/pattern and what's wrong with it, never a vague "needs improvement") — same pattern already required of the Validator.
  - `agent_role/scoper.md` and `agent_role/librarian.md` reviewed and deliberately left unchanged — scoper.md already has extensive, hard-won specific rules (e.g. "NEVER invent or guess a file path", triple goal-restatement), librarian.md already lean and correct; no gap found worth the churn.
  - Templates are loaded at runtime (`std::fs::read_to_string`, not compiled in), so there's no compile-time check of template content — verified instead by `cargo test --lib` (153 passed, 0 failed, unchanged), `cargo clippy --lib --tests` (71 warnings, unchanged baseline), `cargo check --lib --release` clean, and manually re-reading every edited template end-to-end to confirm no leftover/orphaned `{{VAR}}` placeholders.

- [x] `read_tags` now transparently checks whether the repo-root `tags` file is missing or stale and regenerates it before reading, instead of erroring and telling the caller to run ctags separately (2026-08-03, user request). Found a real, severe bug while implementing this: the actual `tags` file in this checkout was **494 MB / 2.5 million lines** — `read_tags`'s own old error message told callers to fix a missing `tags` file by running `ctags -R .` (a raw recursive walk from cwd), and at some point that's exactly what happened, sweeping in this repo's large (37 MB+), gitignored-but-physically-present `docs/` directory of saved reference webpages (`git ls-files` doesn't see it, so `gen_tags.sh`'s own git-tracked-file scoping was never actually the problem — the recursive walk bypassing git entirely was). Fixed properly:
  - `orchestrator::search::{tracked_rust_files, tags_are_stale, regenerate_tags}` (`search.rs`): staleness is `tags` missing or older than any git-tracked `*.rs` file; regeneration is *always* scoped to `git ls-files -- '*.rs'`, fed to `ctags` over stdin (`-L -`) — never a recursive `-R .` walk, so the `docs/`-leak class of bug can't recur regardless of what's sitting in the working tree.
  - `gen_tags.sh` updated to the same safe scope (previously unfiltered `git ls-files`, which would've still tagged every tracked non-Rust file too, just not the actual `docs/` leak — that came from a `-R .` run elsewhere, not this script).
  - Deleted the real 494 MB `tags` file and regenerated it with the fixed scope: **108 KB, 1,088 lines**, zero `docs/` references, 1,052 real `.rs`-file tag entries — confirmed by grep.
  - `ORCHESTRATION.md`'s tool notes previously told readers to generate `tags` via `universal-ctags -R .` — the exact advice that caused this — corrected to describe the new auto-refresh behavior and explicitly warn against `-R .`.
  - Tests: the pure staleness logic (`tags_are_stale`) covered directly with tempfiles/no external dependency; `tracked_rust_files` asserts every result is a real `.rs` path and never under `docs/` (runs unconditionally — this repo is itself a git checkout); the full `regenerate_tags` path is `#[ignore]`d (matching this repo's existing pattern for tests needing an external tool not guaranteed on every dev/CI machine — here, `universal-ctags`) but was run manually against the real `ctags` binary and confirmed passing.

- [x] Fixed a third real failure reported right after the two above (2026-08-03): every `apply_patch` attempt across 3 rounds failed to apply, and it turned out the Worker was writing diffs against a plausible-looking but entirely *hallucinated* function signature (`fn parse_key_val(key: &str, val: &str) -> Result<(String, String), ...>`) instead of the file's real one (`fn parse_key_val<T, U>(s: &str) -> Result<(T, U), ...>`) — it never actually read the file, just guessed from the clippy warning's function name and line number alone. `diffy::apply` correctly rejected the context mismatch each time, but the rejection message was only the raw `diffy` error — nothing telling the Worker what the file actually looks like, so every retry just guessed again. Fixed by including the file's real current content directly in the rejection (`Validation::apply_patch`'s `Err` arm, `agent/protocol.rs`, capped at a new `MAX_SHOWN_ORIGINAL_CHARS` = 4,000 chars) — deliberately *not* just telling the Worker to go call `read_file` and retry, since that costs another round-trip that may not even be available if `retrieve_budget` is already exhausted; showing the content directly lets it write a correct diff on the very next attempt regardless. Also strengthened `agent_role/worker.md`: context lines must match the file's real content exactly, never guess, read the file first if it's not already visible. Regression-pinned with `apply_patch_shows_real_file_content_when_context_does_not_match` against a real tracked file in this repo (`Cargo.toml`, with deliberately unrelated context lines) — confirmed it catches the regression by reverting to the old message and watching the test fail.

- [x] Fixed a second, related real failure reported right after the "no tool call at all" fix above (2026-08-03): the Worker called `cargo_clippy`, got a real result, then called `cargo_clippy` *again* on the reask instead of switching to `apply_patch` — repeated across rounds until the exact-match stall guard escalated the whole run ("Worker stalled: repeated identical output across rounds"). Root cause: `Agent::execute_and_verify` (`agent.rs`) only handles `ApplyPatch`/`Memorize`; any other *valid, recognized* tool reaching it at verify-time (meaning the Worker's one-per-round read-tool reask was ALSO a read-only call, not a patch) fell into a catch-all producing `"Unexpected tool at verify stage: CargoClippy"` — technically correct but not actionable, and easy for a small model to just retry rather than understand. Two-part fix:
  - Improved that rejection message to explicitly state the model already used this round's one lookup and must now emit `apply_patch`/`memorize` — same rejection pathway as the "no tool call at all" fix, extending the same family of clear, deterministic corrections for common Worker mistakes at the verify stage.
  - Added a *proactive* reminder (`orchestrator.rs`'s Stage::Implement, right after a successful read-tool execution, before the reask): a `System` turn stating the tool result is above and the Worker must now act, not call another read-only tool. Heads this off before the mistake happens at all, rather than only correcting it after a rejection — a rule stated once at the top of a long prompt is easy for smaller local models to lose track of by generation time; a reminder immediately before the response point is much more likely to be followed. Also added the equivalent expectation to `agent_role/worker.md` itself ("at most one read-only lookup per round, then you must act").
  - Regression-pinned with `implement_patch_loop_rejects_a_second_read_only_tool_call_with_actionable_guidance` (confirmed it catches the regression by reverting to the old message and watching the test fail). The proactive System-turn reminder itself is not directly unit tested — it lives inline in `run_stage_machine`'s large match arm, not extracted into its own function like `run_implement_patch_loop` was; verified by code review and by the existing `librarian_and_worker_completes`/`worker_and_validator_completes` fixture tests still passing unchanged.

- [x] Fixed a real failure mode reported by the maintainer (2026-08-03): the Worker replied with a narrative markdown walkthrough ("### Identified First Warning... ### Applying the Fix...", "Assuming cargo_clippy has been run...") wrapped around fenced blocks that weren't actually a tool call (a ` ```bash ` block naming a tool like a shell command, then a ` ```rust ` block of raw source, not a diff) instead of emitting a real `apply_patch`. The Validator correctly rejected it ("non-answer: no human available to respond"), but the Architect's next plan was nearly identical to the one that had just failed — the rejection reason reaches the Architect only via the generic `HISTORY` text dump, with no instruction telling it to notice and react to that specific pattern. Three-part fix:
  - **Deterministic root-cause fix** (`orchestrator.rs`): extracted `Stage::Implement`'s patch loop into `Orchestrator::run_implement_patch_loop` (also improves readability of what had become a large match arm), and it now distinguishes `Validation::Skip` — meaning `parse_tool_call` found no recognized tool call anywhere in the Worker's output — on the *first* attempt this round (the Worker did nothing actionable at all) from the *same* `Skip` on a follow-up reask after at least one `apply_patch` already succeeded this round (the Worker's normal way of signaling "I'm done", not a problem). The first case now rejects immediately with a precise, deterministic reason instead of silently proceeding to `Stage::Test` — which would trivially pass since nothing changed — and hoping the Validator LLM happens to correctly diagnose a no-op round. This is more reliable than depending on an LLM to notice, and skips a wasted Test cycle.
  - `agent_role/worker.md`: explicitly forbids narrative walkthroughs (numbered steps, "### Identified..."/"### Applying the Fix" headers, "Assuming X has been run...", "if this resolves it, proceed with the next steps") — the existing "never ask a question" instruction didn't cover this failure mode, since a walkthrough isn't a question.
  - `agent_role/architect.md`: now explicitly instructed to read a Rejection's reason in `HISTORY` before re-planning and, when it indicates the Worker produced prose/narrative instead of an actual change (as opposed to a technical defect like a failing test), state so explicitly in the new plan and tell the Worker plainly to stop narrating and emit a tool call — directly addressing "the Architect should pick this up and tell the Worker to do its job better."

  Regression-pinned with `implement_patch_loop_rejects_a_worker_turn_with_no_tool_call_at_all`, using the exact walkthrough-shaped text from the real failure — confirmed it actually catches the regression by temporarily reverting the fix and watching the test fail. Added `#[derive(Debug)]` to `Validation` (needed by an earlier test's panic message). See the next entry below for two more genuine `apply_patch` diff-parsing gaps (`fix_hunk_header_counts`, a clearer missing-header rejection) found from this same real run.

- [x] Two `apply_patch` diff-parsing robustness fixes, found from a real failed run (2026-08-03, `scripts/refactoring_examples.sh run fix_one_clippy_lint`): the Worker (qwen2.5-coder:14b) emitted a diff with (a) a wrong hunk-header line count (`@@ -3,12 +3,10 @@` claimed 12 old-side lines, the body only had 10) and (b) no `--- a/<file>`/`+++ b/<file>` header lines at all — `diffy::Patch::from_str` rejected the whole thing with a cryptic "hunk header does not match hunk" before either defect could be diagnosed separately. Diagnosed precisely with a throwaway probe test (isolated each defect independently: header-less-but-correct-count parses fine with `original()`/`modified()` both `None`; headers-present-but-wrong-count fails with the same error regardless of headers — confirming the count mismatch, not the missing header, was the actual parse blocker).
  - Added `fix_hunk_header_counts` (`agent/protocol.rs`), run after `normalize_diff_hunk_lines`: recomputes each hunk's count fields from the body's own line prefixes (context lines count toward both sides, `-`/`+` toward one side each). Safe to apply unconditionally — the counts are redundant metadata fully determined by the body, so this can't change what the diff says to add/remove, same category of fix as the existing missing-leading-space repair.
  - For the still-separate missing-header case (no safe way to infer a target file — deliberately not inferring one from the plan's `FILES:` line, keeping `apply_patch`'s existing "resolve from the header, not free text" security posture intact), replaced the `target = patch.original().unwrap_or("unknown")` fallback with an explicit check that gives an actionable rejection message telling the Worker exactly what to add, instead of `unknown` silently flowing into a confusing "not tracked by git" message or the original cryptic parse error.
  - Regression-pinned with the exact diff from the real failure (`apply_patch_gives_an_actionable_message_for_a_diff_with_no_file_header`) plus direct unit tests for `fix_hunk_header_counts` (wrong count corrected, correct count left alone, multiple hunks handled independently). Added `#[derive(Debug)]` to `Validation` along the way — needed for a test assertion's panic message, harmless/generally useful elsewhere.

- [x] Multi-file patches per round (2026-08-03): `Stage::Implement` (`orchestrator.rs`) now loops up to a per-round `patch_budget` of 3 sequential `apply_patch` calls instead of finalizing after exactly one. After each successful patch, `should_continue_patch_loop(ctx, remaining_budget)` — a small pure helper, `remaining_budget > 0 && ctx.planned_files().len() > ctx.pending_patches.len()` — decides whether to re-ask the Worker for the next file; a plan with no `FILES:` line (or exactly one file) always evaluates false after one patch, so single-file rounds are byte-for-byte unaffected. Deliberately count-based rather than matching planned paths against patched ones exactly (would need to expose `protocol.rs::file_in_scope`'s suffix-matching for little practical benefit here). Each `apply_patch` call is still independently subject to the existing tracked-file, scope (`FILES:`), and diff-size checks — nothing about `apply_patch` itself changed, only how many times `Stage::Implement` is willing to call it.

  Required threading `Context::pending_patch: Option<PendingPatch>` into `pending_patches: Vec<PendingPatch>` throughout: `record_patch` now keeps the *first* recorded original for a given path (a second patch to the same file in one round would otherwise read its already-patched content off disk as the "original" — wrong revert target) and `revert_pending_patches` (renamed, was `revert_pending_patch`) rolls back every file the round touched, not just one. `orchestrator/git.rs`'s `commit_add_targets`/`fallback_commit_message` updated to stage/mention every touched file.

  Tests: `should_continue_patch_loop` covered directly (no files planned, exactly one, more planned than patched, budget exhausted, all planned files done) since it's a pure function; `record_patch`/`revert_pending_patches` covered in `types.rs` (first-original-wins semantics; an end-to-end revert test using real temp files via `tempfile::tempdir`, since it's the one part of this that touches actual disk I/O safely). The full multi-file `Stage::Implement` loop itself isn't end-to-end tested — like `commit_feature_branch`, `Validation::apply_patch` runs real `git`/filesystem operations against the process's actual working directory with no injectable path, so exercising two real sequential patches in `cargo test` would mean either mutating this repo's own git-tracked files or extending `apply_patch`/`tracked_files` to accept an explicit working directory, which was out of scope here. Existing single-patch fixture tests (`worker_and_validator_completes` etc.) all still pass unchanged, confirming the backward-compatibility guarantee holds for the common case.

- [x] `cargo_clippy` typed tool (2026-08-03): added `ToolName::CargoClippy` (`agent/tools.rs`) and `orchestrator::cargo::cargo_clippy()`, so the Worker can see clippy's own warnings directly instead of a "fix a clippy lint" task having to guess at what clippy would flag. Worker-only, like `cargo_check`/`cargo_dupes` — deliberately excluded from `prompt_scoper_tool_catalog` (a diagnostic tool mid-Implement, not a repo-fact lookup). Correction to this item's original plan: it assumed `cargo clippy --message-format=json` parsed via `parse_cargo_json_diagnostics` (mirroring `Validation::run_build_and_test`'s *Tester*-facing parsing), but the actual sibling to mirror was `cargo_check()` itself (`orchestrator/cargo.rs`), which turned out to already use plain `--message-format=short` text output, not JSON — this is a Worker-facing tool meant to be read directly, not a source of structured `Diagnostic`s, so matching `cargo_check`'s existing shape was both simpler and more consistent than introducing JSON parsing here. Resource-limited and 30s-timeout-capped exactly like `cargo_check`, via the same `limit_resources`. Tests cover the pure, fast parts (`parse_tool_call` recognizing `cargo_clippy`, and that the Worker catalog offers it while the Scoper catalog doesn't) — the subprocess execution itself isn't unit-tested, consistent with `cargo_check`/`cargo_dupes` having no such test either (slow, environment-dependent, nothing here to fake).

- [x] Two more issues from maintainer feedback on the live-run output above (2026-08-03):
  - **Commit message body lines weren't capped at 80 characters.** The `generate_commit_message` prompt (`orchestrator/git.rs`) only asked for an 80-char wrap in prose, and models don't reliably honor exact character limits. Added a programmatic backstop, `wrap_commit_message_body`/`wrap_line`: hard-wraps every line *after* the first at 80 characters on word boundaries. The summary line is deliberately left untouched — git treats the first line specially (e.g. `git log --oneline`), so wrapping it mid-line would corrupt that convention rather than just look untidy. Applied to both the LLM-generated message and the deterministic fallback.
  - **Missing newline between agents' output** (e.g. `` ```[Architect]: `` glued together with no separator). `render.rs`'s `ColorChange` handler wrote the ANSI code exactly as given, but `Role::get_color()` embeds the next role's banner text with a newline *after* it, never before — so if the previous role's last streamed chunk didn't end in a newline (very common: a fenced code block's closing ` ``` `, a plan's last line), the next role's banner glued directly onto it. Fixed by unconditionally prefixing every `ColorChange` write with `\n`, so every banner (and the plain reset code sent at the end of each turn) always starts on its own fresh line regardless of what was printed just before. Not unit tested — `Io` writes directly to real stdout with no injectable buffer, consistent with the rest of `tui/io.rs`'s tests (which only assert `.is_ok()`, not content) — verified by inspection and by re-reading the exact transcript excerpt that reported the bug.

- [x] Removed redundant per-turn trace noise (2026-08-03, maintainer feedback from the live run above): `query_stream` (`core/agent.rs`) sent a `"[Role's input] querying 'model' — full prompt in .ruchat_trace.md"` trace on *every single turn* (every role, every round) — pure noise once a role's own colored banner (`Role::get_color()`, sent right after) already identifies who's speaking; the model name added nothing repeated across dozens of turns in a multi-round run. `.ruchat_trace.md` still gets refreshed before every query (`ctx.trace(tx, String::new())` — empty message skips the visible announcement but still writes the file, useful when inspecting a stuck run), and a new `Orchestrator::model_summary()` prints which model each configured role uses exactly once, at the very start of a real run (`run_stage_machine`, not the fixture-driven `debug_stage_machine`). Regression-pinned two ways: `model_summary_lists_every_configured_role_once` asserts the summary's content, and `architect_only_completes` now also asserts no trace event contains `"querying"` — confirmed the latter actually catches the regression by temporarily reintroducing the old line and watching it fail.

- [x] Three real bugs found from an actual live run (`ruchat pipe --critic Security --critic Performance ...`, reported by the maintainer) and fixed (2026-08-03):
  - **`commit_feature_branch` staged the entire working tree, not just the AI's change.** `orchestrator/git.rs` ran `git add .`, which staged every dirty/untracked file in the repo onto the auto-generated `ai/feature-*` branch — the user's own unrelated in-progress work, stray files, anything. Fixed with a new `commit_add_targets(ctx)` helper that stages only `featured_changes.md` (the changelog entry the function itself just wrote) plus the single file `apply_patch` touched this run, from `ctx.pending_patch` (`Context` only ever tracks one accepted patch per run — see `PendingPatch`'s doc comment). Regression-pinned with two pure unit tests (`commit_add_targets_*`) since `commit_feature_branch` itself isn't unit tested — it runs real `git` commands against the process's actual working directory, i.e. this repo, so exercising it directly in `cargo test` would mutate this repo's own git state.
  - **Commit messages were a fixed, uninformative `"AI Success: {goal}"`.** Added `generate_commit_message` (`orchestrator/git.rs`): a single direct LLM call (`ollama.chat_stream`, 30s timeout) given the goal and the actual staged diff (`git diff --staged`, captured after the fix above), producing a proper imperative-summary-plus-body message. Deliberately bypasses the Agent/Role/Context turn-log machinery (`agent.rs::query_stream`) — this is a one-shot, non-conversational utility call with nothing worth recording as a Turn. Falls back to a deterministic message (`fallback_commit_message`, goal + changed file) on any failure (Ollama down, timeout, empty response) — a validated, accepted change must never fail to commit just because this nicety failed. Model is configurable via a new `commit_message_model` orchestrator-config key, defaulting to the Worker's model. `Stage::Commit` (`orchestrator.rs`) now passes `self.ollama`/the resolved model through to `commit_feature_branch`.
  - **Concurrently-run Critics' output interleaved into garbled text.** `run_critics_parallel` (`orchestrator.rs`) runs critics via `join_all`, but every critic's `query_stream` was streaming its response token-by-token onto the *same shared* `tx` — with two critics running at once, their `ColorChange`/`Trace`/`ChatChunk` events interleaved on the wire. Since `Role::get_color()` bakes the literal header text into the ANSI string (`"\x1b[1;31m[Critic]:\n"`), both critics announced the identical generic `[Critic]:` header (explaining the duplicated header the user saw), and their token streams then merged with no separator (explaining `"APPROVEDThe rephrased comment is clear..."` with zero whitespace between two critics' verdicts). Fixed by giving each critic its own local channel (drained via `tokio::join!` alongside `query_stream`, not `tokio::spawn`, so no `Send + 'static` bound is needed) instead of the shared `tx`; after `join_all` completes, each critic's full response is emitted as one atomic, distinctly-labeled `ctx.trace` block (`"[Critic 'Security']:\n..."`, `"[Critic 'Performance']:\n..."`) using a new `"name"` field ask.rs now sets from `--critic <name>` (falls back to the `"Critic_N"` role string when unset, e.g. for critics configured via raw `--agentic` JSON). Regression-pinned by a new `run_critics_parallel_emits_one_undamaged_labeled_trace_per_critic` test that calls `run_critics_parallel` directly — the existing `multiple_critics_dispatches_each_critic_once` fixture test does *not* exercise this function at all (fixtures run through `debug_stage_machine`, whose separate `"Critic_N"` sequence branch calls `query_stream` directly and sequentially, never had this bug, and was confirmed to still pass unchanged when the real fix was temporarily reverted — caught this before trusting the wrong test as coverage). That new test was itself briefly flaky (~1 in 3 full-suite runs): it originally asserted *which* critic got *which* canned response, but both critics race on `FakeLlmClient`'s shared scripted-response queue (a plain `VecDeque`, popped in call order) now that they genuinely run concurrently — a real, unavoidable non-determinism given the fix being tested, not a code bug. Fixed by asserting structure instead (exactly one trace per critic, each clearly labeled, each containing exactly one critic's response) rather than a specific critic-to-response mapping; verified with 15 consecutive full-suite runs after the fix, 0 failures (vs. 1 failure within the first 3 runs before it).

- [x] Fixed two secret-leaking log lines (2026-08-03): `Orchestrator::new`'s Librarian setup (`orchestrator.rs`) and `AskArgs::into_config` (`ollama/ask.rs`) both logged the raw config string verbatim via `tracing::error!(config = %s, ...)` when it failed to parse as JSON — `s`/`json_str` there is the `chroma_client`/`--agentic` config text, which can legitimately embed `chroma_token` (a real secret, `CHROMA_TOKEN`), so a malformed-but-token-bearing config leaked the token into logs at default log levels. Fixed by dropping the raw string from both log lines — the `serde_json`/`anyhow` parse error already carries enough line/column context to debug without echoing the secret-bearing text. Regression-pinned with `agentic_parse_failure_does_not_log_the_raw_config` (`ask.rs`) and `librarian_chroma_client_parse_failure_does_not_log_the_raw_config` (`orchestrator.rs`) — each installs a capturing `tracing_subscriber::fmt` writer via `tracing::subscriber::with_default` and asserts a planted secret never appears in the captured output. Note for the orchestrator.rs test specifically: it must run `Orchestrator::new` on a **current-thread** tokio runtime (`Builder::new_current_thread()`, not `Runtime::new()`'s default multi-thread) — `with_default` sets a thread-local subscriber, so if the traced code runs on a different worker thread the assertion is silently vacuous (nothing captured, so "contains no secret" trivially holds even with the bug present); caught this by deliberately reintroducing the old code and confirming the test failed before trusting it. Two new (pre-existing-category) `clippy::result_large_err` warnings appeared at the two fixed closures — same already-tolerated lint as the ~46 existing instances elsewhere in the crate (`RuChatError`'s size, not something this fix changed), not treated as new debt.
- [x] Refreshed `comparisons/{AUTOGEN,CREWAI,LANGCHAIN,LANGGRAPH}_COMPARISON.md` against the current codebase (2026-08-03) — they'd drifted significantly: all four still described a "SHELL" tool that never existed as a generic tool (deliberately designed out, see the architecture note in `CLAUDE.md`) and a flat `Context` (`history`/`documents`/`rejections` strings) that was replaced by the append-only `Turn` log a while ago. Corrected those, added the Scoper role and RAG reranking/memory-recall to the relevant rows, and added a new **Safety / Sandboxing** comparison row to each file (typed tool catalog, path containment, `apply_patch`'s tracked-file/diff-cap/scope checks, `RLIMIT`-capped cargo subprocesses) — a real differentiator none of the four previously credited. Doing this surfaced the `OutputArgs` default bug documented below. Also identified two genuine, comparison-driven roadmap gaps (resumable runs, interactive HITL) — added to `ROADMAP.md` Phase 3 and mirrored into the High Priority section above.
- [x] **Found and fixed a bug that made the Librarian's RAG retrieval silently render as empty text on every real run** (2026-08-03), while implementing automatic memory recall below (see next entry) — its own `librarian_only_completes`/`librarian_and_worker_completes` tests only asserted the output stream was non-empty, never that retrieved content actually appeared in it, so this had gone unnoticed. Root cause: `OutputArgs` (`providers/vector/chroma.rs`) derived `Default`, but its `fields` column-selection has a *clap* `default_value = "id,doc,meta"` — clap's `default_value`/`default_value_t` attributes only take effect through `Parser::parse_from`, not `#[derive(Default)]`, so `OutputArgs::default().fields` was silently `vec![]` (and `sort` was silently `false` instead of the documented `true`). Every non-CLI construction path hits this: `Query::default()` in `orchestrator.rs`'s `run_librarian_retrieval` (the real, live Librarian retrieval path — its LLM-authored query JSON never specifies `"output"`, so it always fell through to the broken default) and the new `recall_prior_memories`. With an empty `fields` list, `render_markdown`'s `if cols.is_empty() { return String::new() }` (and `render_oneliner` likewise) meant `query_collection` succeeded, `rerank_query_results` ran, but the rendered result handed to the Worker/Architect was always `""` — the Librarian has probably never actually delivered retrieved document text to a prompt in production, despite `ROADMAP.md`/`CLAUDE.md` listing RAG reranking as shipped and working. Fixed with a manual `impl Default for OutputArgs` matching the real CLI defaults exactly, instead of the derive. Added `output_args_default_matches_documented_cli_defaults` and `query_default_output_args_renders_document_content` (`providers/vector/chroma.rs`) to regression-pin it.
- [x] Automatic memory recall at session start (2026-08-03): `Orchestrator::recall_prior_memories` (`core/orchestrator.rs`), called once from `run_stage_machine` right after `Context::new`, before `Stage::Scope` begins. Deterministic — builds a `Query` from `ctx.goal` directly (`n_results: 3`) rather than an LLM-authored query spec like the Librarian's own on-demand `run_librarian_retrieval`, since there's no other context yet at session start to reason about narrowing the query further. Reuses the already-configured Librarian's Chroma client and `embed_model` (gated on both being present — a no-op otherwise, see the new High Priority item above for lifting that requirement), and pushes results as a `TurnKind::Retrieval` turn tagged `"Memory"` (distinguishable from `"Librarian"` in traces, though both feed `Context::documents_view` identically). Never fails the run: a query error (e.g. the very first run, before anything has ever been memorized, so the collection doesn't exist yet) is traced and swallowed, not propagated — absence of prior memories is the normal case. Only wired into the real `run_stage_machine`, not `debug_stage_machine` (the fixed-sequence debug/test mechanism) — adding an implicit extra step there would break the fixtures' determinism contract. Tested directly against `recall_prior_memories` (not via a fixture) using `build_test_orchestrator` + `FakeVectorStore`, covering both the recall-happens and no-librarian-means-no-op cases.
- [x] Resource-limited sandboxing for cargo subprocesses (2026-08-03): `cargo_check`/`cargo test`/`cargo tree --duplicates` had wall-clock timeouts (30s/60s/120s/20s) but no memory/CPU caps. Added `orchestrator::cargo::limit_resources(cmd, wall_clock_secs)` — sets `RLIMIT_AS` (4 GiB, a fixed ceiling) and `RLIMIT_CPU` (`wall_clock_secs * available_parallelism()`, since CPU *time* on a parallel build can exceed wall-clock time several-fold) via an `unsafe { cmd.pre_exec(...) }` closure that only calls `libc::setrlimit` (async-signal-safe, no allocation) — before spawning `cargo` in `orchestrator/cargo.rs::cargo_check` and all three cargo spawns in `agent/protocol.rs` (`run_cargo_check`, and both halves of `run_build_and_test`). Rlimits are inherited across fork/exec, so this also bounds every rustc/build-script/proc-macro process cargo itself forks, not just the top-level `cargo` process. `cargo_dupes` (`cargo tree --duplicates`) wasn't given a limit — it doesn't compile anything. Unix-only (`setrlimit` is POSIX; no-op stub on non-unix, though the project doesn't currently target Windows). Added `libc` as a direct dependency (`Cargo.toml`) — it was already resolved transitively, so this doesn't add a new crate to the dependency tree. Verified with an end-to-end test that spawns a real `sh -c 'ulimit -v; ulimit -t'` child and asserts the kernel actually enforced the requested limits, not just that `pre_exec` was registered.
- [x] Exposed `BuildReport::parsed_diagnostics` to the rejection path instead of only a flattened diagnostics string (2026-08-03): added `BuildReport::rejection_message()` (`agent/protocol.rs`), which renders compile errors from `parsed_diagnostics` first (each citing its exact `file:line:col` via `Diagnostic`'s `Display`), then any non-blocking warnings, then whatever `diagnostics` (raw `cargo test` stdout, or a stderr/timeout fallback for `cargo check` output that couldn't be parsed as JSON) still carries — `orchestrator.rs`'s `Stage::Test` now calls this instead of pushing the raw `report.diagnostics` string as the Tester's Rejection turn. Removed the `#[allow(dead_code)]` on `parsed_diagnostics` since it's genuinely read now. Fixed a real bug found in the process: the old code rendered warnings-only compiles as an empty string (comment said "keep informational" but the code discarded them via `String::new()`), so if `cargo test` then failed for an unrelated reason, those warnings never reached the Worker at all — `rejection_message` now always surfaces them from `parsed_diagnostics` directly.
- [x] Fixed `model_options` file/string merge being a silent no-op (2026-08-03): `merge_options_json`'s config-value gate used to check `defaults.contains_key(&k)` against `serde_json::to_value(ModelOptions::default())`, but every `ModelOptions` field is `Option` with `skip_serializing_if = "Option::is_none"`, so a default instance always serializes to `{}` — the gate was never true for any key, and config-supplied `model_options` values silently landed in the discarded `remain` map instead of `defaults`, never reaching the final `ModelOptions`. Fixed by gating on a new `MODEL_OPTION_KEYS` allowlist (`cli/options.rs`) — the real field set, discovered by compiling against `ollama_rs::models::ModelOptions`'s builder methods (13 fields: `num_ctx`, `temperature`, `top_k`, `top_p`, `repeat_penalty`, `stop`, `num_predict`, `seed`, `mirostat`, `mirostat_eta`, `mirostat_tau`, `tfs_z`, `repeat_last_n` — several originally-guessed Ollama options like `presence_penalty`/`num_gpu`/`min_p` don't exist on this crate version, 0.3.6, and were dropped after the compiler rejected them) — rather than the crate's own (unusable for this purpose) default-serialization shape. Also fixed a related bug the fix surfaced: `serde_json::Map`'s `IndexMut` panics on a missing key instead of inserting one, so the merge itself needed `.insert()`, not `defaults[&k] = v`. The old regression test (`model.rs::config_only_model_options_are_currently_silently_dropped`, which asserted the broken behavior) is now `config_only_model_options_are_applied` and asserts the fix; added two more covering CLI-flags-plus-config together and a non-field key being ignored rather than silently passed through.
- [x] `apply_patch` scope check against the Architect's plan (2026-08-03): the Architect prompt (`agent_role/architect.md`) now instructs it to end a file-touching plan with a `FILES: path1, path2` line; `Context::planned_files` (`agent/types.rs`) parses that line from the latest Plan turn, and `Validation::apply_patch` (`agent/protocol.rs`) rejects a patch whose diff-header target isn't in that list (exact or path-suffix match, `file_in_scope`), same rejection pattern as the existing not-tracked-by-git check. Deliberately fail-open when the plan has no `FILES:` line at all (empty `planned_files()` skips the check) rather than fail-closed, since local models don't reliably emit new prompt conventions — a missing line doesn't retroactively block every patch, but a plan that *does* name files now enforces them. The Scoper role wasn't given a matching scope declaration: its `information_needed` list is read-only lookups, not files it intends to modify, so there was nothing there to enforce against.
- [x] **v0.2.0 released** (2026-08-03) — version bumped in `Cargo.toml`/`README.md`; this migration of completed items out of the priority sections and into this list is that release's own checklist item. `cargo check --lib`, `cargo test --lib`, `cargo clippy --lib --tests`, and `cargo build --release` all verified clean (pre-existing dead-code warnings only, no new ones) before the bump.
- [x] Fixed a flaky test: `cli::options::tests::test_read_options_file` and `test_get_options_with_file` both wrote/read/deleted the same relative path (`test_options.json`) and ran concurrently under the default multi-threaded test runner, racing on the shared file (intermittent `EOF while parsing a value`). Switched both to `tempfile::tempdir()` (the pattern already used in `cli/config.rs`/`cli/prompt.rs`), giving each test its own isolated file.
- [x] Removed the double JSON round-trip in `ModelArgs::build_generation_request` — it called `get_options` (which already does `ModelOptions::default()` → JSON → merge → `ModelOptions`), then serialized that `ModelOptions` back to JSON a second time just to merge in CLI flags, then deserialized again. Extracted `options::merge_options_json` (the pre-deserialize half of `get_options`) so `build_generation_request` merges CLI flags directly onto the same JSON `Value` and deserializes once. (Surfaced the `model_options` no-op bug above in the process — tracked separately, not fixed here.)
- [x] Implement global config file with profile support — turned out to already exist and work (`cli/config.rs::ConfigArgs`, `~/.config/ruchat/config.json`/`./ruchat.json`/`--config`/`RUCHAT_CONFIG`, `"profiles": {name: {...}}` selected via `--profile`/`RUCHAT_PROFILE`, tested by `test_config_json_profile`). JSON, not TOML, but the item's own wording always allowed either. Also deleted `cli/serde.rs::read_config_file`, a second, dead, never-called JSON-file-reader that duplicated part of this — found via the pre-existing dead-code clippy warning.
- [x] Migrated genuine diagnostic `eprintln!`/`println!` call sites in `src/core`/`src/providers` to `tracing`. The rest of the `println!`s in that tree (`chroma/ls.rs`, `ollama/server.rs::ls`, `manager.rs`, `chroma/query.rs`'s result print) are each command's actual designed stdout output, not debug prints — converting those to `tracing` would hide them by default without `RUST_LOG` set, so they're staying as-is
- [x] Fixed the concrete cases found where an error handler actively discarded useful diagnostic info (`map_err(|_| ...)`) rather than just using a generic variant name: `model.rs::get_model_name_inner` was reporting "model not found" for what could be an unreachable-Ollama-server failure, now includes the real cause and an "is Ollama running?" hint; `tools.rs`'s `ToolParseError::UnknownTool` was a unit variant with no way to tell the caller (or the Worker retry loop) which invalid tool name the model actually output, now carries it, and is split from a new `MissingTool` for the genuinely-no-tool-field case. Also replaced a `.unwrap()` inside `func_struct`'s interactive chat loop (`ollama/func/strukt.rs`) that would crash the whole process on any transient chat failure with `?` (the function already returns `Result`); and removed ~8 `if x.is_string() { x.as_str().unwrap() }`-shaped unwraps across `agent.rs`/`chroma.rs`/`collection.rs`/`include.rs`/`where.rs` in favor of `if let Some(s) = x.as_str()` (same behavior, no unwrap). Audited `ollama_rs`'s chat-stream error path too — its `Item` error type is `()`, no more detail is recoverable there, left as-is.
- [x] Unit tests for parser modules: `include.rs`/`where.rs` had internal parse functions covered but not the `IncludeArgs`/`WhereArgs` `parse()`/`update_from_json()` wrappers CLI code actually calls (now added); `cli/prompt.rs` (`andify_list`, `get_prompt`, `promptless`) had zero tests, now covered including the external-command exit-code path
- [x] `cargo test --lib` was uncompilable (33 errors: `OutputArgs`/`create_table` test code hadn't been updated after a prior refactor to `format`/`render_rows`, and two `where.rs` tests compared a `Result<T>` against a bare `T` after `map_sql_comparison`/`map_sql_to_document_op` started returning `Result`) and, separately, `test_handle_request_default` called `Args::parse_from(["test", "-h"])`, which makes clap call `std::process::exit(0)` mid-test-run — silently killing every other test in the same process depending on thread scheduling. All fixed; the suite is green.
- [x] **Multi-critic consensus review was completely non-functional.** `Orchestrator::new`'s Critics loop passed each critic's flat config object straight to `Agent::new` as `config`, which looks up `config.get(role)` — a key that can never exist in a flat object, so `Agent::new` always errored and `critics` stayed empty regardless of `--critic`/`"Critics"` config, silently. Even fixed, a second bug meant `query_stream` would then fail with `InvalidRole`: `Role::from_str` only recognized the bare string `"critic"`, never the `"Critic_0"`/`"Critic_1"` naming `Orchestrator::new` actually assigns. Both fixed (`orchestrator.rs`, `agent/role.rs`); caught by wiring `agent_debug/multiple_critics.json` into a real test (`core::orchestrator::tests::multiple_critics_dispatches_each_critic_once`) — nothing had ever exercised this path end-to-end before.
- [x] Wired 9 of 10 `agent_debug/*.json` fixtures into `cargo test --lib` (`core::orchestrator::tests::*`) using a new `FakeLlmClient` (`agent/llm_client.rs` — the `FakeVectorStore`/`FakeEmbeddingsClient` fakes already existed but had never actually been wired to anything) alongside the existing `FakeVectorStore`, so the stage machine can be exercised without a live Ollama/Chroma server. Also fixed a fixture bug: `critic.json`/`multiple_critics.json` used `"Critic0"`/`"Critic1"` (no underscore), which doesn't match the `"Critic_N"` naming the code actually expects.
- [x] Added `.github/workflows/ci.yml`: build + `cargo clippy --lib --tests` + `cargo test --lib` on push/PR. Deliberately no `-D warnings` (dead-code warnings not yet cleaned up) and no `fmt --check` yet (repo-wide drift not yet cleaned up).
- [x] Investigated connection pooling for Ollama and Chroma clients — turned out to already be satisfied, not a gap: `Orchestrator::new` (`core/orchestrator.rs`) constructs exactly one `Ollama` client and, when a Librarian is configured, exactly one Chroma client per run, wraps each in `Arc`, and shares that single instance across every agent role (Architect/Worker/Validator/Critics/Summarizer/Librarian) rather than each role independently constructing its own. Neither `ollama_rs::Ollama` nor `chroma::ChromaHttpClient` are wrapped with a custom `reqwest::Client::builder()` anywhere in this codebase that could disable pooling, so both get `reqwest`'s default keep-alive/idle-pool behavior for free. The standalone one-shot CLI subcommands (`chroma-get`, `chroma-search`, etc.) each make exactly one HTTP request per process invocation, so there's nothing to pool against within their lifetime anyway.
- [x] Consolidated TODO files into single `TODO.md`
- [x] Improved model option merging with CLI flags
- [x] env_logger / tracing integration
- [x] Basic multi-agent orchestration with RAG support
- [x] Git auto-commit feature branch on approval
- [x] Robust Chroma CLI with where/include parsing
- [x] TUI chat with history, undo/redo, selection — **later removed** (2026-07-31, `ad0708d` and surrounding commits deleted the ~1,260-line `providers/llm/ollama/chat/{conversation_tree,history,pos,event_result}.rs` this was built on); no longer accurate as a "done" claim, see the "TUI Chat" section above
- [x] Structured tool calling framework (`agent/tools.rs::ToolName`, schema-validated, replaces regex-only parsing) — 13 typed tools including `apply_patch`, `git_*`, `read_file`, `ripgrep`, `read_tags`, `cargo_check`/`cargo_dupes`
- [x] Parallel critic execution (`Orchestrator::run_critics_parallel`, `futures_util::future::join_all`)
- [x] RAG relevance scoring / reranking (`providers/vector/chroma/rerank.rs`, distance+lexical blend)
- [x] Token-aware history management with automatic Summarizer trigger (`Stage::Retry`, `get_dynamic_history_limit`)
- [x] Pre-planning repo-grounding stage (`Scoper` role — not in the original TODO/ROADMAP list at all)
- [x] Structured `Context` event log (`Vec<Turn>` + `TurnKind`) replacing the old flat-string `history`/`context`/`documents`/`rejections` fields
- [x] Reconciled the legacy `Team`/`Manager` pipeline — `ruchat manager` now runs a saved `Team` preset through the real `Orchestrator` stage machine instead of a separate, unvalidated linear engine
- [x] `apply_patch` diff-size cap (`MAX_PATCH_DIFF_BYTES`, `agent/protocol.rs`) and automatic rollback of a rejected round's patch before looping back to `Plan` (`Context::{record_patch,revert_pending_patch}`)
- [x] Confirmed the "remove dead code" item once flagged above for `conversation_tree.rs`/legacy `Team`/`Manager` is fully resolved: `conversation_tree.rs` no longer exists and `team.rs`/`manager.rs` are the reconciled implementation this list already credits — removed the stale duplicate bullet
- [x] Removed an unused `OrchestratorRun` struct (`orchestrator.rs`) whose doc comment described bundling an `Orchestrator` to implement `AgentPipeline`'s "fixed `run(&mut self, ...)` signature" — `AgentPipeline` (`agent/pipeline.rs`) is an enum with its own `run(self)`, not a trait anything implements, so both the struct and its rationale were stale leftovers; `ask.rs`/`manager.rs` already construct `AgentPipeline::Orchestrator` directly

---

**Next milestone:** v0.3.0 (ROADMAP.md Phase 2) — further RAG improvements (document summarization, multi-collection queries) and automatic Chroma collection management. (`apply_patch` scope-check, the `model_options` merge fix, exposing `parsed_diagnostics`, resource-limited cargo subprocess sandboxing, automatic memory recall, and the `OutputArgs` default-empty-render bug are now done, see above. Recall is Librarian-gated for now — see the High Priority item above.) Looking further out to v0.4.0 (ROADMAP.md Phase 3), two new comparison-driven targets were added: resumable/crash-resilient runs and an interactive human-in-the-loop approval gate.

Help welcome on any item — especially testing and configuration refactoring.
